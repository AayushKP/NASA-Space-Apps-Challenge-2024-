import React from "react";
import { ChevronRight } from "lucide-react";

const NewsCard = ({ title, description, imageUrl, altText }) => {
  return <div className="w-full font-sans"></div>;
};

export default NewsCard;
